# Database Configuration (Local Development)
DB_USER=postgres
DB_PASS=your_local_password_here
DB_NAME=honeypot_db
DB_HOST=localhost
DB_PORT=5432

# Database Configuration (Google Cloud SQL)
# Uncomment and fill these in for production deployment
# INSTANCE_CONNECTION_NAME=your-project-id:us-central1:honeypot-db

# Application Configuration
NODE_ENV=development
PORT=8080

# Session Secret (generate a random 32+ character string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-random-session-secret-min-32-chars-CHANGE-THIS

# Admin PIN Reference (actual PIN is stored in database via seeder)
# Default PIN: 3591
# To change: Update the seeder file or modify directly in database
